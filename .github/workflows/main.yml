name: M3U Generation Task (to GitFlic)

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'm3u_task/**'
      - '.github/workflows/m3u-generator.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout Push-Engine
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      - name: Run Playlist Generation
        run: |
          cd m3u_task
          python generate_playlists.py

      - name: Deploy to GitFlic
        env:
          GF_TOKEN: ${{ secrets.GITFLIC_PUSH_ENGINE_TOKEN }}
          # --- EDIT THESE TWO LINES ---
          GF_REPO: "gitflic.ru/YOUR_USERNAME/YOUR_REPO_NAME.git"
          GF_USER: "YOUR_USERNAME"
          # ----------------------------
        run: |
          # 1. Create a fresh temporary folder for the upload
          mkdir -p upload_dir
          
          # 2. Copy only the results from the task folder
          if [ -d "m3u_task/playlists" ]; then
            cp -r m3u_task/playlists/* ./upload_dir/
          else
            echo "Error: playlists directory not found!"
            exit 1
          fi
          
          cd upload_dir
          
          # 3. Initialize a clean git environment
          git init
          git config user.name 'PushEngine-Bot'
          git config user.email 'engine@push-engine.local'
          
          # 4. Prepare the files
          git add .
          git commit -m "Automated Update: $(date +'%Y-%m-%d %H:%M')"
          
          # 5. Direct push using the token (bypasses 'remote origin exists' error)
          git push "https://${{ env.GF_USER }}:${{ env.GF_TOKEN }}@${{ env.GF_REPO }}" master --force
